[project]
name = "agent-builder-class5-hw1-jaikishen"
version = "0.1.0"
description = "NL-to-SQL pipeline + multi-tool agent notebooks."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain-core>=1.4.0",
    "langchain-openai>=1.2.1",
    "pandas>=3.0.3",
]
